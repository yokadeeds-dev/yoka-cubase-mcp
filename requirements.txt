# --- Core (MCU-Layer + MCP-Server) ---
mido>=1.3.0
python-rtmidi>=1.5.8
mcp>=1.0.0
python-osc>=1.9.0                        # OSC-Bridge (runtime/osc/)

# Windows: AHK-Bridge (Window-Guard + Key-Send, runtime/ahk/_win_impl.py)
# Environment-Marker: wird auf macOS/Linux nicht installiert.
pywin32>=306; sys_platform == "win32"

# --- Nicker Audio-Analyse (LUFS / Spektrum / True-Peak, runtime/persona/audio_analytics.py) ---
numpy>=1.26
scipy>=1.11
soundfile>=0.12
pyloudnorm>=0.2
